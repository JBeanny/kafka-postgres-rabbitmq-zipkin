# KAFKA SERVER, POSTGRESQL SERVER , PGADMIN , RABBITMQ AND ZIPKIN

    you can choose on your demands just by copying the docker-compose.yml and run with the following command :

```bash
docker compose up -d
```