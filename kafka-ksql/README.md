# USING KAFKA WITH KSQL SERVER AND CONFLUENT CLOUD

To run the server you can type the following command: 

```bash
docker compose up -d
```

source: https://docs.confluent.io/platform/current/platform-quickstart.html